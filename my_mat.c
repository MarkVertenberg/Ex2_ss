#include <stdio.h>
#include "my_mat.h"

int** insertToMatrix(){
    int matrix[N][N];
    for(int i = 0; i < N; i++){
        for(int j = 0; j < N; j++){
            scanf("%d", &matrix[i][j]);
        }
    }
    return matrix;
}

int isTherePath(int i, int j){
    return 0;
}

int fastestPath(int i, int j){
    return 0;
}